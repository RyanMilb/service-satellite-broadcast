import * as grpc from 'grpc';
import { Client } from './client';
import * as Type from './typedef/event';
export declare type StreamRequest = {
    filter: {
        hash?: string;
        instanceHash?: string;
        key?: string;
    };
};
export default class Event extends Client {
    constructor(endpoint: string);
    stream(request: StreamRequest, signature: string): grpc.ClientReadableStream<Type.mesg.types.IEvent>;
}
