import { Client } from './client';
import * as API from './api/runner';
export declare type RegisterRequest = {
    serviceHash: string;
    envHash: string;
};
export declare type DeleteRequest = {
    runnerHash: string;
};
export default class Runner extends Client {
    constructor(endpoint: string);
    register(request: RegisterRequest, signature: string): Promise<API.mesg.grpc.orchestrator.IRunnerRegisterResponse>;
    delete(request: DeleteRequest, signature: string): Promise<API.mesg.grpc.orchestrator.IRunnerRegisterResponse>;
}
