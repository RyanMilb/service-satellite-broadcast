import LCDClient from './util/lcd';
export declare type IInstance = {
    hash: string;
    serviceHash: string;
    envHash: string;
};
export default class Instance extends LCDClient {
    get(hash: string): Promise<IInstance>;
    list(filter?: {
        serviceHash?: string;
    }): Promise<IInstance[]>;
}
