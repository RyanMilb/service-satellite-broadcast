import { Response } from 'node-fetch';
export declare type IPaginationResponse = {
    page?: number;
    limit?: number;
    totalCount?: number;
};
export default class LCDClient {
    private _endpoint;
    constructor(endpoint?: string);
    protected query(path: string, data?: any, method?: 'POST' | 'GET'): Promise<{
        height: string;
        result: any;
        pagination?: IPaginationResponse;
    }>;
    protected getRequest(path: string, params?: any): Promise<Response>;
    protected postRequest(path: string, data?: Object): Promise<Response>;
    private fullEndpoint;
}
